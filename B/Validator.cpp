#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;

int main(int argc, char* argv[]){
    registerValidation(argc, argv);
    int T = inf.readInt(1,100,"T");
    inf.readEoln();
    for(;T--;inf.readEoln()){
    	int n = inf.readInt(1,100,"N");
    	for(int i=0;i<2*n;++i)
    		int x = inf.readInt(-5000,5000,"x");
    }
    inf.readEof();
}